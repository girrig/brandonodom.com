# Gmail address that sends (and by default receives) the contact-form emails
GMAIL_USER=you@gmail.com

# 16-char Google App Password for that account (not your normal password)
GMAIL_APP_PASSWORD=your-16-char-app-password

# Optional: deliver messages to a different inbox than GMAIL_USER
# CONTACT_RECIPIENT=someone-else@example.com
